# 리랭커 모델 교체 결정 리포트 (T6 — P3 결정 규칙 실행)

- **날짜**: 2026-08-10 22:31 (KST)
- **작업**: `log-issue-fixes` T6 — FlashRank 리랭커 모델 교체 vs 유지 결정
- **현재 모델**: `ms-marco-MultiBERT-L-12` (config.yml `rag.reranker.model_name`)
- **후보 모델**: `ms-marco-MultiMiniLM-L-12-v2`
- **인터프리터**: `C:\ProgramData\anaconda3\envs\rag-ollama\python.exe` (flashrank 0.2.0 — 실제 설치 경로 `%APPDATA%\Python\Python310\site-packages\flashrank`, onnxruntime 1.20.1)
- **결정**: **교체하지 않음 (KEEP)** — 후보 모델 사용 불가 (다운로드 404 + 설치판 지원 목록에 없음)

---

## 1. 후보 모델 가용성 결과: **사용 불가**

`Ranker(model_name="ms-marco-MultiMiniLM-L-12-v2", cache_dir=".model_cache")` 로드 시도 결과 **예외 발생**:

```
Traceback (most recent call last):
  File "<string>", line 1, in <module>
  File "C:\Users\darkzard05\AppData\Roaming\Python\Python310\site-packages\flashrank\Ranker.py", line 36, in __init__
Downloading ms-marco-MultiMiniLM-L-12-v2...
    self._download_model_files(model_name)
  File "C:\Users\darkzard05\AppData\Roaming\Python\Python310\site-packages\flashrank\Ranker.py", line 52, in _download_model_files
    r.raise_for_status()
  File "C:\ProgramData\anaconda3\envs\rag-ollama\lib\site-packages\requests\models.py", line 1167, in raise_for_status
    raise HTTPError(http_error_msg, response=self)
requests.exceptions.HTTPError: 404 Client Error: Not Found for url: https://huggingface.co/prithivida/flashrank/resolve/main/ms-marco-MultiMiniLM-L-12-v2.zip
```

**근거 (2중 확인, 추측 아님):**

1. **설치판 flashrank 0.2.0 지원 모델 목록** (`flashrank/Config.py` — `model_file_map` 원문):
   ```python
   model_file_map = {
       "ms-marco-TinyBERT-L-2-v2":    "flashrank-TinyBERT-L-2-v2.onnx",
       "ms-marco-MiniLM-L-12-v2":     "flashrank-MiniLM-L-12-v2_Q.onnx",
       "ms-marco-MultiBERT-L-12":     "flashrank-MultiBERT-L12_Q.onnx",
       "rank-T5-flan":                "flashrank-rankt5_Q.onnx",
       "ce-esci-MiniLM-L12-v2":       "flashrank-ce-esci-MiniLM-L12-v2_Q.onnx"
   }
   ```
   `ms-marco-MultiMiniLM-L-12-v2`는 **지원 목록에 없음** (다운로드가 통과해도 `model_file_map[model_name]`에서 `KeyError` 발생).

2. **HuggingFace 직접 확인** (HEAD 요청):
   | 대상 | HTTP 상태 |
   |---|---|
   | 후보 `.../ms-marco-MultiMiniLM-L-12-v2.zip` | **404 Not Found** |
   | 현재 `.../ms-marco-MultiBERT-L-12.zip` | 302 (존재) |

**영향**: 후보 모델 벤치마크/품질 평가 **실행 불가** — 후보의 mean_ms·P@1·MRR@5는 **측정 불가 (N/A)**.

---

## 2. 벤치마크 테이블 (실제 후보 풀 — 2201.07520v1.pdf 실제 검색 청크 18개, 질의 "cm3가 뭔가요?", repeat=5)

| model | mean_ms | median_ms | p95_ms | 근거 JSON |
|---|---|---|---|---|
| `ms-marco-MultiBERT-L-12` (현재, 기준치 T5) | **4581.23** | 4538.74 | 4768.44 | `reports/rerank_bench_20260810_182338.json` |
| `ms-marco-MultiBERT-L-12` + `OMP_NUM_THREADS=12` (튜닝 재벤치) | **5309.67** | 5322.14 | 5407.67 | `reports/rerank_bench_20260810_222357.json` |
| `ms-marco-MultiMiniLM-L-12-v2` (후보) | **N/A** (로드 실패) | N/A | N/A | — |

- 세션 정보 (양쪽 동일): providers=`['CPUExecutionProvider']`, intra_op_num_threads=`0`(auto), inter_op_num_threads=`0`, graph_optimization_level=`ORT_ENABLE_ALL`.
- `OMP_NUM_THREADS=12` 설정 후에도 ORT 세션 스레드 설정은 `intra_op=0`(auto)로 **변화 없음** — OMP 환경변수는 이 ORT 빌드에서 세션 스레드에 반영되지 않았고, 실측도 기준치 대비 **악화** (5309.67 vs 4581.23 ms).
- **Ranker API 튜닝 레버 없음**: 설치판 `Ranker.__init__` 시그니처는 `(model_name, cache_dir, max_length)`뿐 — 세션 옵션(intra/inter-op 스레드 등) 노출 없음.

---

## 3. 품질 테이블 (golden_set 기반, `--testset_n 3 --no-llm`, scorable=4건 — 행 4 "안녕, 너는 누구니?" 퇴화 제외)

| model | P@1 | MRR@5 | context_recall | 근거 JSON |
|---|---|---|---|---|
| `ms-marco-MultiBERT-L-12` (현재) | **0.0** | **0.1458** | 0.5556 (4건 중 3건 판정) | `reports/eval_quality_qr_current_20260810_222629.json` |
| `ms-marco-MultiMiniLM-L-12-v2` (후보) | **측정 불가** (모델 로드 실패) | **측정 불가** | 측정 불가 | — |

현재 모델 골든셋 질문별 상세 (P@1/MRR@5):
| golden row | 질문 요약 | P@1 | MRR@5 | 관련 문서 존재 | max_rerank |
|---|---|---|---|---|---|
| 1 | 이미지 학습 원리·토큰화·DALL-E 차이 | 0 | 0.3333 | True (3순위) | 0.918 |
| 2 | 파라미터 수·데이터셋 규모 | 0 | 0.0 | False | 0.0753 |
| 3 | 핵심 기여점 3가지 | 0 | 0.0 | False | 0.9085 |
| 5 | 이미지 생성 최신 트렌드 (out-of-doc) | 0 | 0.25 | True (4순위) | 0.0719 |

> 참고: `--no-llm` 모드로 generation/judge 미실행 (TTFT/TPS/judge는 집계 대상 아님 — 리랭크 품질 비교 목적만 사용).

---

## 4. 결정 규칙 계산 (기계적, task 사양)

**규칙**: 교체 ⇔ (후보 mean_ms ≤ 4581.23/2 = **2290.6 ms**) AND (후보 품질 미하락: P@1 동일, MRR@5 ±0.05 이내)

| 게이트 | 조건 | 후보 값 | 판정 |
|---|---|---|---|
| G1 속도 | mean_ms ≤ 2290.6 | **측정 불가** (모델 다운로드 404 → 로드 실패, 벤치 실행 불가) | **미충족** (값이 존재하지 않아 부등식 성립 불가) |
| G2 품질 | P@1 동일 AND MRR@5 ±0.05 | **측정 불가** (모델 로드 실패 → eval 실행 불가) | **미충족** |

**결과**: AND 조건으로 **두 게이트 모두 통과 불가 → 교체 규칙 성립하지 않음 → KEEP**.

---

## 5. 선택 경로: **유지(KEEP) + 세션 튜닝 시도**

- **config.yml 변경 없음** — `rag.reranker.model_name: "ms-marco-MultiBERT-L-12"`, `rag.reranker.engine: "auto"` 유지.
- **튜닝 시도 (bounded, 1회 재벤치)**: `$env:OMP_NUM_THREADS="12"` (머신 논리 코어 12개) → 평균 **5309.67 ms** (기준치 4581.23 ms 대비 **개선 없음, 오히려 악화**). ORT 세션 `intra_op_num_threads`는 여전히 auto(0) — 환경변수 레버 미반영.
- **Ranker API 튜닝 레버**: **없음** — `Ranker(model_name, cache_dir, max_length)`만 노출, 세션 옵션 파라미터 부재. → `reports/t6_tune_bench.txt` (튜닝 재벤치 stdout 캡처).
- **후속 권고 (범위 외, 기록만)**: 교체 가능한 대안 후보가 설치판 flashrank 지원 목록에 존재함 — `ms-marco-MiniLM-L-12-v2`(영어 전용 12레이어, 속도 개선 후보) 및 `rank-T5-flan`. 단, **영어 전용 모델은 한국어 질의/문서에 품질 하락 위험**이 있어 이번 결정 규칙 대상(다국어 후보)에서는 제외. 다국어 속도 개선은 후보 부재로 현 상태 유지.

---

## 6. config.yml 상태 (before/after)

```yaml
# BEFORE (= AFTER, 변경 없음)
rag:
  reranker:
    # [수정] 다국어 지원을 위해 모델 교체 (기존 TinyBERT는 영어 전용)
    model_name: "ms-marco-MultiBERT-L-12"
    engine: "auto"
```

- `git diff config.yml` → **빈 출력 (변경 0건)**. 결정 문서와 config 상태 일치.

---

## 7. 산출물

| 파일 | 내용 |
|---|---|
| `reports/rerank_decision_20260810_223130.md` | 본 문서 (결정 규칙 + 수치 + 선택 경로) |
| `reports/rerank_bench_20260810_182338.json` | T5 기준치 (실제 풀, mean 4581.23 ms) |
| `reports/rerank_bench_20260810_222357.json` | 튜닝 재벤치 (OMP_NUM_THREADS=12, mean 5309.67 ms) |
| `reports/t6_tune_bench.txt` | 튜닝 재벤치 stdout 캡처 |
| `reports/eval_quality_qr_current_20260810_222629.json` / `.md` | 현재 모델 품질 평가 (P@1 0.0, MRR@5 0.1458) |
| `reports/t6_eval_current.txt` | 품질 평가 stdout 캡처 |

- 후보 벤치 stdout(`t6_candidate_bench.txt`): **생성 안 함** — 후보 로드 실패로 벤치 미실행 (1절의 예외가 유일한 후보 증거).
