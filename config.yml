# config.yml
# 애플리케이션의 주요 설정을 관리하는 파일입니다.

# --- 모델 설정 ---
models:
  # 기본으로 사용할 Ollama 모델 이름
  default_ollama: "qwen3:4b"
  
  # Ollama 서버 주소 (로컬: http://127.0.0.1:11434, 도커: http://host.docker.internal:11434)
  base_url: "http://127.0.0.1:11434"

  # Ollama 모델의 토큰 생성 제한 (기본값: 4096)
  # -1로 설정하면 모델의 기본 설정 또는 무제한(컨텍스트 윈도우까지)으로 생성합니다.
  # 명시적으로 충분한 길이를 확보하여 답변 끊김 방지
  ollama_num_predict: -1

  # Ollama 모델의 온도 설정 (기본값: 0.1)
  temperature: 0.1

  # Ollama 모델의 컨텍스트 윈도우 설정 (기본값: 4096)
  num_ctx: 4096

  # Ollama 모델의 탑-피 설정 (기본값: 0.8)
  top_p: 0.8

  # 사용 가능한 임베딩 모델 목록
  available_embeddings:
    - "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2"
    - "intfloat/multilingual-e5-large-instruct"
    - "sentence-transformers/paraphrase-multilingual-mpnet-base-v2"
    - "sentence-transformers/all-MiniLM-L6-v2"
  
  # 모델 캐시 디렉터리
  cache_dir: ".model_cache"

  # 임베딩 모델의 배치 크기 ('auto'로 설정 시 VRAM에 따라 자동 조절, 숫자로 직접 지정 가능)
  embedding_batch_size: "auto"

# --- RAG 파이프라인 설정 ---
rag:
  # 벡터 저장소 캐시 디렉터리
  vector_store_cache_dir: ".model_cache/vector_store_cache"
  
  # 리트리버 설정
  retriever:
    search_type: "similarity"
    search_kwargs:
      k: 5
    # BM25와 FAISS의 가중치 (Ensemble Retriever용)
    ensemble_weights: [0.4, 0.6]

  # 재순위화(Reranking) 설정
  reranker:
    enabled: false  # 로컬 환경 속도를 위해 기본값 비활성화 (필요시 true로 변경)
    model_name: "BAAI/bge-reranker-v2-m3" # 한국어/다국어 지원 모델로 변경
    top_k: 3  # 최종적으로 LLM에 전달할 문서 개수 (5 -> 3으로 줄여 속도 향상)

  # 텍스트 분할 설정
  text_splitter:
    chunk_size: 500
    chunk_overlap: 100
  
  # 의미론적 분할 설정 (임베딩 기반)
  semantic_chunker:
    enabled: true
    breakpoint_threshold_type: "similarity_threshold"
    breakpoint_threshold_value: 0.6
    sentence_split_regex: "(?<=[.!?])\\s+"
    min_chunk_size: 200
    max_chunk_size: 1000
    similarity_threshold: 0.6

  # 쿼리 확장 설정
  query_expansion:
    enabled: false  # 로컬 LLM(4B 이하)의 파싱 오류 및 속도 저하 방지를 위해 비활성화

  # 프롬프트 설정
  prompts:
    qa_system_prompt: |
      당신은 사용자가 제공한 PDF 문서를 기반으로 정확하고 상세한 답변을 제공하는 AI 문서 분석 전문가입니다.
      아래의 [참고 문서] 내용을 철저히 분석하여 사용자의 질문에 답변하세요.

      **답변 작성 가이드라인 (중요도 순):**
      0. **언어 절대 일치 (Language Consistency):**
         - **사용자의 질문이 한국어라면 반드시 한국어로 답변하세요.**
         - **사용자의 질문이 영어라면 반드시 영어로 답변하세요.**
         - 참고 문서가 영어로 되어 있더라도, 질문이 한국어라면 내용을 번역하여 한국어로 설명해야 합니다. 이 규칙은 타협할 수 없습니다.

      1. **상세하고 풍부한 답변:**
         - 질문에 대해 단순히 요약하지 말고, 문서의 근거를 바탕으로 충분히 상세하게 설명하세요.
         - 문서의 맥락을 파악하여 사용자가 내용을 깊이 이해할 수 있도록 정보를 최대한 활용하세요.

      2. **철저한 문서 기반 답변 (엄격 준수):**
         - 제공된 [참고 문서]에 있는 내용만을 근거로 답변하세요.
         - 문서에 없는 내용은 **절대로 추측해서 만들어내지 말고**, "제공된 문서에는 해당 내용이 없습니다"라고 솔직하게 말하세요.
         - 외부 지식(당신의 사전 학습 데이터)보다 [참고 문서]의 내용을 항상 최우선으로 하세요. 이를 어길 시 답변은 실패로 간주됩니다.

      3. **명확한 출처 표기:**
         - 답변의 신뢰성을 위해, 가능한 경우 정보의 출처(페이지 번호 등)를 본문 내에 인용하세요. (예: "R1 모델은 32B 파라미터를 가집니다 [p.12]")
         - **답변 끝에 별도의 '참고 문서' 또는 '출처' 목록을 작성하지 마세요.** (시스템이 자동으로 추가합니다.)

      4. **구조적이고 가독성 높은 형식:**
         - 답변은 줄글로만 작성하지 말고, **Markdown** 형식을 적극 활용하세요.
         - 핵심 내용은 **불렛 포인트(-)** 나 **번호 매기기(1.)** 를 사용하여 정리하세요.
         - 중요한 키워드나 문장은 **굵게(**)** 표시하여 강조하세요.

      5. **답변의 논리적 구성 (두괄식 필수):**
         - 답변의 **첫 문단은 질문에 대한 직접적이고 명확한 결론 요약**으로 시작하세요.
         - "이 문서는..." 같은 불필요한 서두를 줄이고 바로 핵심 내용을 전달하세요.
         - 그 뒤에 '상세 설명', '근거', '예시' 등을 구조적으로 덧붙이세요.

    # 쿼리 확장(Multi-Query) 프롬프트: 검색 최적화 전략 적용
    query_expansion_prompt: |
      당신은 검색 최적화 AI 도우미입니다. 사용자의 질문에 더 정확히 답변하기 위해, 원본 질문을 다각도로 분석하여 **3개의 검색용 쿼리**를 생성하세요.

      ### 쿼리 생성 전략
      1. **핵심 개념 정의**: 질문의 핵심 키워드가 무엇인지 묻는 쿼리
      2. **세부 문맥 확장**: 질문의 구체적인 의도나 연관된 세부 내용을 묻는 쿼리
      3. **대안적 표현**: 같은 의미를 가진 다른 용어(동의어)나 영어/한국어 대역어를 포함한 쿼리

      ### 출력 형식
      - 오직 3개의 쿼리만 한 줄에 하나씩 출력하세요.
      - 번호, 부가 설명, 인용 부호(")를 절대 포함하지 마세요.
      
      ### 예시
      사용자: "R1 모델의 성능은 어때?"
      출력:
      R1 모델 성능 벤치마크 결과
      DeepSeek R1 주요 기능 및 특징
      R1 모델과 이전 버전 성능 비교

# --- 채팅 UI 설정 ---
ui:
  # 채팅 및 PDF 뷰어 컨테이너의 높이
  container_height: 500
  
  # UI에 표시될 메시지
  messages:
    # --- 일반 메시지 ---
    preparing_answer: "답변 생성 준비 중..."
    thinking: "🤔 생각을 정리하는 중입니다..."
    no_thought_process: "아직 생각 과정이 없습니다."
    no_related_info: |
      제공된 문서에서 질문에 대한 명확한 정보를 찾기 어렵습니다. 😥

      **다음을 시도해 보세요:**
      - 질문을 좀 더 명확하게 하거나 다른 키워드를 사용해 보세요.
      - 좀 더 일반적이거나 넓은 범위의 질문을 해보세요.

    # --- 사이드바 ---
    sidebar_title: "⚙️ 설정"
    pdf_uploader_label: "PDF 파일 업로드"
    model_selector_label: "LLM 모델 선택"
    embedding_selector_label: "임베딩 모델 선택"
    system_status_title: "📊 시스템 상태"
    loading_models: "LLM 모델 목록을 불러오는 중..."

    # --- PDF 뷰어 ---
    pdf_viewer_title: "📄 PDF 미리보기"
    pdf_viewer_no_file: "미리볼 PDF가 없습니다. 사이드바에서 파일을 업로드해주세요."
    pdf_viewer_prev_button: "← 이전"
    pdf_viewer_next_button: "다음 →"
    pdf_viewer_page_slider: "페이지 이동"
    pdf_viewer_error: "PDF를 표시하는 중 오류가 발생했습니다: {e}"

    # --- 채팅 ---
    chat_title: "💬 채팅"
    chat_input_placeholder: "PDF 내용에 대해 질문해보세요."
    chat_no_qa_system: "QA 시스템이 준비되지 않았습니다. PDF를 먼저 처리해주세요."
    chat_welcome: |
      **RAG-Chat에 오신 것을 환영합니다!**

      사이드바에서 PDF 파일을 업로드하여 문서 내용에 대한 대화를 시작해보세요.
    chat_guide: |
      **💡 사용 가이드**
      - **PDF 업로드:** 좌측 사이드바에서 분석하고 싶은 PDF를 업로드하세요.
      - **모델 선택:** 로컬 `Ollama` 모델을 선택할 수 있습니다.
      - **질문하기:** 문서 처리가 완료되면, 내용에 대해 자유롭게 질문할 수 있습니다.
      - **PDF 뷰어:** 우측에서 원본 문서를 함께 보며 대화할 수 있습니다.
      
      **⚠️ 알아두실 점**
      - **답변의 정확성:** 답변은 업로드된 PDF 내용만을 기반으로 생성되며, 사실이 아닐 수 있습니다.
      - **초기 로딩:** 임베딩 모델을 처음 사용하면 다운로드에 몇 분이 소요될 수 있습니다.
    
    # --- 오류 및 상태 메시지 ---
    streaming_error: "스트리밍 답변 생성 중 오류 발생: {e}"
    generic_error: "오류가 발생했습니다: {error_msg}"
    retry_button: "재시도"
    errors:
      ollama_not_running: "Ollama 서버에 연결할 수 없습니다. 서버가 실행 중인지 확인해주세요."