# RAG 파이프라인 최적화 컨텍스트 노트

## 2026-06-10: 설계 및 전략 수립
- **배경:** 사용자가 시스템의 병목 및 중복 로직 식별을 요청함.
- **결정:**
    1. LLM Grading 노드가 가장 큰 시간 병목이므로 리랭커 점수를 활용한 Short-circuit 도입 결정. (Threshold: 0.85)
    2. 인덱싱 단계에서 의미론적 분할과 중복 제거가 분리되어 있어 연산 낭비가 심함 -> 1-Pass 통합 결정.
    3. 리랭킹 후보군을 고정 25개로 두는 것은 자원 낭비이므로 Score Gap 기반 동적 조절 도입 결정.
- **기술적 근거:** FlashRank(TinyBERT)는 로컬 CPU에서 매우 빠르고 신뢰할 수 있는 시맨틱 점수를 제공함. 이를 LLM 평가의 보조 지표가 아닌 선제적 판단 지표로 격상시켜 Latency를 개선함.
- **위험 요소:** Threshold가 너무 낮으면 부적절한 문서가 통과될 수 있으므로, 0.85를 기본값으로 하되 향후 설정 가능하게 고려함.
